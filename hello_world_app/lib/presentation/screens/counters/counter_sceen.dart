import 'package:flutter/material.dart';

class CounterSceen extends StatelessWidget {
  const CounterSceen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Counter screen')));
  }
}
